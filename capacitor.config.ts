import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'backdrop-tree-shaking',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
